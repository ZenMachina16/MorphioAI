import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geist = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ContentRepurposer - AI-Powered Social Media Content Creator",
  description: "Transform long-form content into engaging social media posts for X (Twitter), LinkedIn, and Instagram with AI. Save hours of content creation time.",
  keywords: "content repurposing, social media, AI content, Twitter posts, LinkedIn posts, Instagram captions",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geist.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
